[project]
name = "hl-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0",
    "hyperliquid-python-sdk>=0.9",
    "python-dotenv>=1.0",
    "playwright>=1.60.0",
]

[tool.uv]
package = false
